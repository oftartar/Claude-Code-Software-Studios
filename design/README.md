# design/

Design artifacts. Owned by `design-lead`, `ux-designer`, `design-systems-engineer`.

```
design-system/   tokens.md, theming.md, inventory.md   (/setup-design-system)
ux/              principles.md, <flow>.md              (/ux-design)
wireframes/      low-fi layouts / diagrams
```

Component implementation lives in `src/components/`; this directory is the spec
side. UX specs are reviewed with `/ux-review`.
